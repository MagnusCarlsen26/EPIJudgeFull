{
  "scripts": {
    "build": "npm run build:data && npm run build:config",
    "build:config": "node scripts/write-config.mjs",
    "build:data": "node scripts/build-data.mjs",
    "dev": "python3 -m http.server 5173 --directory public"
  },
  "devDependencies": {}
}
